export function saveState(key: string, value: string) {
  // Placeholder stub for saving state
  return { key, value };
}

export function loadState(key: string): string | null {
  // Placeholder stub for loading state
  return key ? null : null;
}
